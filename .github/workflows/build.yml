name: Build switch-pctltcp-sysmodule

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: devkitpro/devkita64:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install switch-tools (provides elf2nsp)
        run: |
          pacman -Suy --noconfirm
          pacman -S --noconfirm switch-tools

      - name: Build
        run: |
          make clean
          make -j$(nproc)
          echo "=== Output files ==="
          ls -la *.nsp *.elf 2>/dev/null || true

      - name: List output
        run: |
          echo "=== All .nsp files ==="
          find . -name "*.nsp" -ls 2>/dev/null || echo "No .nsp files found"
          echo "=== Build dir ==="
          ls -la build/ 2>/dev/null || true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: pctltcp-sysmodule
          path: pctltcp-sysmodule.nsp
          if-no-files-found: error
